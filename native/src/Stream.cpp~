#include "Stream.h"

class MemoryStream : public BaseStream {
 private:
  char *buff;
  uint size, pointer;
 public:
  MemoryStream(char *buff, uint size);
  uint read(uint size) override;
  void write(char *buff, uint size) override;
  bool available() override;
  bool seek(uint pos) override;
};
